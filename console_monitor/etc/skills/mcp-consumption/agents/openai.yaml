interface:
  display_name: "Console Monitor MCP"
  short_description: "Read monitored console output"
  default_prompt: "Use $mcp-consumption to inspect a monitored console through MCP."

dependencies:
  tools:
    - type: "mcp"
      value: "console-monitor"
      description: "Console monitor MCP server"
